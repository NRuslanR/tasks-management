spring:
  application:
    name: ToDos App Test

  datasource:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: false
    database: h2

  h2:
    console:
      enabled: false